//WAP fr a variable z with datatype short int
#include<stdio.h>

void main()
{
    //Declare x and y as int varia. and z as short int varia.
    int x, y;
    short int z;
    
    // Assign two 6 digit numbers to x and y
    x = 123456;
    y = 654321;

    // Assign sum of x and y to z
    z = x + y;

    // Output the value of x , y and z
    printf("\n x = %d", x);
    printf("\n y = %d", y);
    printf("\n z = %d", z);
}